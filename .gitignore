*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
desktop.ini
@eaDir

# Real employee claim samples for analysis — never commit (contains PII)
/claim-samples

# Database dumps / backups — contain real data, never commit
*.sql

# Claude Code local tooling config (personal, per-machine — not shared)
/.claude/settings.json
/.claude/settings.local.json
